const testName: string = " user can sign in";
const timeOut = 30;
const shouldRetry = true;

let responseBody: any = '{ status:"ok" }';

if (typeof responseBody === "number") {
    console.log(responseBody);
    
}else{
    console.log("Hapipy hklmk !");
}
